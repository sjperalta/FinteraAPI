<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <title><%= I18n.t("devise.mailer.password_change.subject") %></title>
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        color: #2c3e50;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 40px 20px;
        margin: 0;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.15);
        overflow: hidden;
      }
      .header {
        background: linear-gradient(135deg, #27ae60 0%, #229954 100%);
        padding: 30px 20px;
        text-align: center;
      }
      .header h1 {
        color: #ffffff;
        margin: 0;
        font-size: 28px;
        font-weight: 600;
      }
      .content {
        padding: 30px;
      }
      .greeting {
        font-size: 18px;
        color: #27ae60;
        font-weight: 600;
        margin-bottom: 15px;
      }
      .intro {
        color: #34495e;
        line-height: 1.6;
        margin-bottom: 25px;
      }
      .success-box {
        background: linear-gradient(135deg, #27ae60 0%, #229954 100%);
        color: white;
        padding: 20px;
        border-radius: 8px;
        margin: 25px 0;
        text-align: center;
      }
      .success-box .icon {
        font-size: 32px;
        margin-bottom: 10px;
      }
      .footer {
        background: #f8f9fa;
        padding: 20px;
        text-align: center;
        color: #7f8c8d;
        font-size: 12px;
        border-top: 1px solid #e9ecef;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <h1>🔑 <%= I18n.t("devise.mailer.password_change.subject") %></h1>
      </div>

      <div class="content">
        <p class="greeting"><%= I18n.t("devise.mailer.password_change.greeting", email: @resource.email) %></p>

        <div class="success-box">
          <div class="icon">✅</div>
          <p><%= I18n.t("devise.mailer.password_change.confirmation") %></p>
        </div>

        <p class="intro"><%= I18n.t("devise.mailer.password_change.note") %></p>
      </div>

      <div class="footer">
        <p><%= I18n.t("mailers.footer.message") %></p>
      </div>
    </div>
  </body>
</html>
