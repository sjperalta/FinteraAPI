<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <title><%= I18n.t("mailers.user_mailer.contract_submitted.title") %></title>
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        color: #2c3e50;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 40px 20px;
        margin: 0;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.15);
        overflow: hidden;
      }
      .header {
        background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
        padding: 30px 20px;
        text-align: center;
      }
      .header h1 {
        color: #ffffff;
        margin: 0;
        font-size: 28px;
        font-weight: 600;
      }
      .content {
        padding: 30px;
      }
      .greeting {
        font-size: 18px;
        color: #3498db;
        font-weight: 600;
        margin-bottom: 15px;
      }
      .intro {
        color: #34495e;
        line-height: 1.6;
        margin-bottom: 25px;
      }
      .details-card {
        background: #f8f9fa;
        border-radius: 8px;
        padding: 20px;
        margin: 20px 0;
      }
      .detail-row {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        border-bottom: 1px solid #e9ecef;
      }
      .detail-row:last-child {
        border-bottom: none;
      }
      .detail-label {
        color: #7f8c8d;
        font-size: 14px;
      }
      .detail-value {
        color: #2c3e50;
        font-weight: 600;
        text-align: right;
      }
      .status-box {
        background: #fff3cd;
        border-left: 4px solid #ffc107;
        padding: 15px;
        margin: 20px 0;
        border-radius: 4px;
      }
      .status-box strong {
        color: #856404;
      }
      .cta-button {
        display: block;
        width: fit-content;
        margin: 30px auto;
        padding: 15px 40px;
        background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
        color: white;
        text-decoration: none;
        border-radius: 25px;
        font-weight: 600;
        text-align: center;
        box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
        transition: transform 0.2s;
      }
      .cta-button:hover {
        transform: translateY(-2px);
      }
      .footer {
        background: #f8f9fa;
        padding: 20px;
        text-align: center;
        color: #7f8c8d;
        font-size: 12px;
        border-top: 1px solid #e9ecef;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <h1>📝 <%= I18n.t("mailers.user_mailer.contract_submitted.title") %></h1>
      </div>

      <div class="content">
        <p class="greeting"><%= I18n.t("mailers.user_mailer.contract_submitted.greeting", name: @contract.applicant_user.full_name || @contract.applicant_user.email) %></p>

        <p class="intro"><%= I18n.t("mailers.user_mailer.contract_submitted.created_line", project: @contract.lot.project.name) %></p>

        <div class="status-box">
          <strong>⏳ <%= I18n.t("mailers.user_mailer.contract_submitted.footer") %></strong>
        </div>

        <div class="details-card">
          <div class="detail-row">
            <span class="detail-label">📍 <%= I18n.t("mailers.user_mailer.contract_submitted.labels.project") %></span>
            <span class="detail-value"><%= @contract.lot.project.name %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">🏘️ <%= I18n.t("mailers.user_mailer.contract_submitted.labels.lot") %></span>
            <span class="detail-value"><%= @contract.lot.name %> - <%= @contract.lot.address %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">💳 <%= I18n.t("mailers.user_mailer.contract_submitted.labels.financing") %></span>
            <span class="detail-value"><%= I18n.t("financing_types.#{@contract.financing_type}") %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">📅 <%= I18n.t("mailers.user_mailer.contract_submitted.labels.term") %></span>
            <span class="detail-value"><%= I18n.t("mailers.user_mailer.contract_submitted.term_months", term: @contract.payment_term) %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">🏦 <%= I18n.t("mailers.user_mailer.contract_submitted.labels.reserve") %></span>
            <span class="detail-value"><%= number_to_currency(@contract.reserve_amount) %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">💰 <%= I18n.t("mailers.user_mailer.contract_submitted.labels.down_payment") %></span>
            <span class="detail-value"><%= number_to_currency(@contract.down_payment) %></span>
          </div>
        </div>

        <a href="<%= ENV.fetch('APP_URL', 'https://app.fintera.com') %>/dashboard" class="cta-button"><%= I18n.t("mailers.user_mailer.contract_submitted.view_platform") %></a>
      </div>

      <div class="footer">
        <p><%= I18n.t("mailers.user_mailer.footer.message") %></p>
        <p style="margin-top: 10px; color: #95a5a6;">
          <%= I18n.t("mailers.user_mailer.contract_submitted.created_at", datetime: @contract.created_at.strftime("%d/%m/%Y %H:%M")) %>
        </p>
      </div>
    </div>
  </body>
</html>