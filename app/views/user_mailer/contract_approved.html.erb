<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <title><%= I18n.t("mailers.user_mailer.contract_approved.title") %></title>
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        color: #2c3e50;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 40px 20px;
        margin: 0;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.15);
        overflow: hidden;
      }
      .header {
        background: linear-gradient(135deg, #27ae60 0%, #229954 100%);
        padding: 30px 20px;
        text-align: center;
      }
      .header h1 {
        color: #ffffff;
        margin: 0;
        font-size: 28px;
        font-weight: 600;
      }
      .content {
        padding: 30px;
      }
      .greeting {
        font-size: 18px;
        color: #27ae60;
        font-weight: 600;
        margin-bottom: 15px;
      }
      .intro {
        color: #34495e;
        line-height: 1.6;
        margin-bottom: 25px;
      }
      .details-card {
        background: #f8f9fa;
        border-radius: 8px;
        padding: 20px;
        margin: 20px 0;
      }
      .detail-row {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        border-bottom: 1px solid #e9ecef;
      }
      .detail-row:last-child {
        border-bottom: none;
      }
      .detail-label {
        color: #7f8c8d;
        font-size: 14px;
        margin-right: 15px;
      }
      .detail-value {
        color: #2c3e50;
        font-weight: 600;
        text-align: right;
      }
      .highlight-box {
        background: linear-gradient(135deg, #27ae60 0%, #229954 100%);
        color: white;
        padding: 20px;
        border-radius: 8px;
        margin: 25px 0;
        text-align: center;
      }
      .highlight-box .amount {
        font-size: 32px;
        font-weight: 700;
        margin: 10px 0;
      }
      .highlight-box .label {
        font-size: 14px;
        opacity: 0.9;
      }
      .payment-info {
        background: #fff3cd;
        border-left: 4px solid #ffc107;
        padding: 15px;
        margin: 20px 0;
        border-radius: 4px;
      }
      .payment-info strong {
        color: #856404;
      }
      .cta-button {
        display: block;
        width: fit-content;
        margin: 30px auto;
        padding: 15px 40px;
        background: linear-gradient(135deg, #27ae60 0%, #229954 100%);
        color: white;
        text-decoration: none;
        border-radius: 25px;
        font-weight: 600;
        text-align: center;
        box-shadow: 0 4px 15px rgba(39, 174, 96, 0.3);
        transition: transform 0.2s;
      }
      .cta-button:hover {
        transform: translateY(-2px);
      }
      .footer {
        background: #f8f9fa;
        padding: 20px;
        text-align: center;
        color: #7f8c8d;
        font-size: 12px;
        border-top: 1px solid #e9ecef;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <h1>🎉 <%= I18n.t("mailers.user_mailer.contract_approved.title") %></h1>
      </div>

      <div class="content">
        <p class="greeting"><%= I18n.t("mailers.user_mailer.contract_approved.greeting", name: @contract.applicant_user.full_name || @contract.applicant_user.email) %></p>

        <p class="intro"><%= I18n.t("mailers.user_mailer.contract_approved.approved_line", project: @contract.lot.project.name) %></p>

        <div class="details-card">
          <div class="detail-row">
            <span class="detail-label">📍 <%= I18n.t("mailers.user_mailer.contract_approved.labels.project") %></span>
            <span class="detail-value"><%= @contract.lot.project.name %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">🏘️ <%= I18n.t("mailers.user_mailer.contract_approved.labels.lot") %></span>
            <span class="detail-value"><%= @contract.lot.name %> - <%= @contract.lot.address %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">💳 <%= I18n.t("mailers.user_mailer.contract_approved.labels.financing") %></span>
            <span class="detail-value"><%= I18n.t("financing_types.#{@contract.financing_type}") %></span>
          </div>
          <% if @contract.financing_type == 'direct' %>
            <div class="detail-row">
              <span class="detail-label">📅 <%= I18n.t("mailers.user_mailer.contract_approved.labels.term") %></span>
              <span class="detail-value"><%= I18n.t("mailers.user_mailer.contract_approved.term_months", term: @contract.payment_term) %></span>
            </div>
            <div class="detail-row">
              <span class="detail-label">💰 <%= I18n.t("mailers.user_mailer.contract_approved.labels.down_payment") %></span>
              <span class="detail-value"><%= number_to_currency(@contract.down_payment) %></span>
            </div>
          <% end %>
          <div class="detail-row">
            <span class="detail-label">🏦 <%= I18n.t("mailers.user_mailer.contract_approved.labels.reserve") %></span>
            <span class="detail-value"><%= number_to_currency(@contract.reserve_amount) %></span>
          </div>
        </div>

        <% if @contract.financing_type == 'direct' %>
          <% first_payment = @contract.payments.where(payment_type: 'installment').order(:due_date).first %>
          <div class="highlight-box">
            <div class="label"><%= I18n.t("mailers.user_mailer.contract_approved.monthly_payment") %></div>
            <div class="amount"><%= number_to_currency(first_payment&.amount || 0) %></div>
          </div>
        <% else %>
          <div class="highlight-box">
            <div class="label"><%= I18n.t("mailers.user_mailer.contract_approved.balance") %></div>
            <div class="amount"><%= number_to_currency(@contract.amount - @contract.reserve_amount) %></div>
            <div style="font-size: 12px; margin-top: 10px; opacity: 0.9;">
              <%= I18n.t("mailers.user_mailer.contract_approved.financed_note") %>
            </div>
          </div>
        <% end %>

        <% if @contract.financing_type == 'bank' %>
          <% first_payment = @contract.payments.where(payment_type: 'installment').order(:due_date).first %>
          <div class="payment-info">
            <strong>🗓️ <%= I18n.t("mailers.user_mailer.contract_approved.first_payment_due", date: first_payment&.due_date&.strftime("%d/%m/%Y") || 'N/A') %></strong>
          </div>
        <% end %>

        <a href="<%= ENV.fetch('APP_URL', 'https://app.fintera.com') %>/dashboard" class="cta-button"><%= I18n.t("mailers.user_mailer.contract_approved.view_platform") %></a>
      </div>

      <div class="footer">
        <p><%= I18n.t("mailers.user_mailer.footer.message") %></p>
        <p style="margin-top: 10px; color: #95a5a6;">
          <%= I18n.t("mailers.user_mailer.contract_approved.approved_at", datetime: @contract.approved_at.strftime("%d/%m/%Y %H:%M")) %>
        </p>
      </div>
    </div>
  </body>
</html>